import React from 'react';

const header = props => {
    return(
        <div style={{backgroundColor : '#F95700FF',
         color : 'white',
          lineHeight: '90px',
          width: '100%',
          fontSize: '30px',
          textAlign: 'center'
        }}>
        A Weather App
        </div>
    );
}

export default header;